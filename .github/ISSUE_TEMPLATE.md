# Public Issue Guidance

Use the structured issue forms when possible.

Do not include API keys, webhook secrets, bearer tokens, session cookies,
private keys, provider credentials, database URLs with passwords, raw provider
signatures, raw payload bodies, customer data, or exploit payloads. Use
placeholders, hashes, redacted logs, and synthetic fixtures.
