[project]
name = "webhookery"
version = "0.1.0"
description = "Minimal Python client for Webhookery control-plane APIs"
requires-python = ">=3.10"
authors = [{name = "Webhookery"}]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
